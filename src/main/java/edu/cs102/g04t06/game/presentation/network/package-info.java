/**
 * Networking presentation package for LAN multiplayer support.
 *
 * <p>The intended design is server-authoritative: one host owns the match state,
 * remote players send commands, and all clients render server snapshots.</p>
 */
package edu.cs102.g04t06.game.presentation.network;
